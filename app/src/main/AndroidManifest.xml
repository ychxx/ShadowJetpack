<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yc.shadowjetpack">

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" /> <!-- 安装权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!--获取设备网络状态，禁用后无法获取网络状态-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShadowJetpack">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestShowLoadingActivity" />
        <activity android:name=".TestSpecialReleaseActivity" />
        <activity android:name=".TestSpecialReleaseActivity2" />
        <activity android:name=".TestRefreshActivity" />
        <activity android:name=".TestWidgetActivity" />
        <activity android:name=".chart.TestChartLineActivity" />
        <activity android:name=".TestPickerViewActivity" />
        <activity android:name=".socket.TestSocketActivity" />
        <activity android:name=".TestRefreshActivity2" />
        <activity android:name=".TestRefreshActivity3" />
        <activity android:name=".TestRemovePagingDataActivity" />

    </application>

</manifest>